/* 
 * Defines aliases for JQuery effects, so that Wijmo components continue to work correctly.
 */

$.effects.blind = $.effects.blind || $.effects.effect.blind;
$.effects.bounce = $.effects.bounce || $.effects.effect.bounce;
$.effects.clip = $.effects.clip || $.effects.effect.clip;
$.effects.drop = $.effects.drop || $.effects.effect.drop;
$.effects.explode = $.effects.explode || $.effects.effect.explode;
$.effects.fade = $.effects.fade || $.effects.effect.fade;
$.effects.fold = $.effects.fold || $.effects.effect.fold;
$.effects.highlight = $.effects.highlight || $.effects.effect.highlight;
$.effects.pulsate = $.effects.pulsate || $.effects.effect.pulsate;
$.effects.scale = $.effects.scale || $.effects.effect.scale;
$.effects.shake = $.effects.shake || $.effects.effect.shake;
$.effects.slide = $.effects.slide || $.effects.effect.slide;
$.effects.transfer = $.effects.transfer || $.effects.effect.transfer;
